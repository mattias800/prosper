# Baseline for tools/env/check_diag_gates.py -- diagnostics whose PRODUCER and PRINTER are
# armed by different PROSPER_* switches (#2149).
#
# One line per finding: KEY  # CLASS: note. The checker FAILS on a finding missing from this file
# (a newly introduced split gate) AND on a line here that no longer reproduces (the fix landed --
# delete the row). Both halves matter: a baseline nobody is forced to update stops describing the
# tree, which is the failure class this tool exists to catch.
#
# THE KEY is KIND|file|subject|requirement. It deliberately contains neither a line number nor the
# membership of a long alternation -- `(any)` stands for any clause of more than three alternatives
# -- so that neither ordinary code motion nor adding a variable to a bulk `PROSPER_DUMP_*`-style
# predicate re-keys a row. Both are churn doors that would fail the Docs job on a PR that did
# nothing wrong, and whose obvious repair -- regenerate the baseline -- silently downgrades every
# classification below. `A+B` means both clauses are required; `A|B` means either satisfies one.
#
# CLASSES
#   defect       a confirmed instance of #2149's shape; the note names the issue.
#   config-echo  the field echoes CONFIGURATION, so 0 truthfully means "unconstrained". That is
#                the shape #2143 recommends, not the defect.
#   benign       read and judged sound, with the reason in the note: the rule's shape matches but
#                nothing here can print a field it did not measure. A `benign` note must name the
#                MECHANISM at a file:line the next reader can open, and the issue it was judged
#                under -- "looks fine" is not a class. Since #2572 that is ENFORCED (rule 4 in
#                baseline_integrity()), because `benign` is the one class whose entire content IS
#                an argument, and an argument is what a hurried rebase replaces with a stub.
#   unreviewed   found by the sweep and NOT judged. Honest debt; #2572 worked the last 79 down to
#                zero, so a row carrying this class again is new debt, not inherited debt.
#
# There is deliberately no class meaning "the variables share a name family, so it is probably
# fine". Membership of a name family is triage only -- #2149's fourth instance was PROSPER_PROGRESS
# + PROSPER_PROGRESS_UNIMPL, which IS a name family, and the family is exactly what made the
# coupling invisible: the name promised what only the pair delivered. A shared family cannot be the
# end of an argument, so it does not get to be a class that reads like a clearance.
#
# WHAT A `benign` TWO-GATE ROW DOES AND DOES NOT SAY (#2572). 47 of the 79 rows reviewed there were
# settled by showing that one of the two clauses is not a requirement at all -- it is an opt-OUT
# tested negated, a parameter with a compiled-in default, a mode that is on by default, or a scanner
# alias artifact. That answers the TWO-GATE question completely: no, arming the switch whose name
# matches your question does NOT leave you in silence. It says nothing about whether the remaining
# single-gated diagnostic can print an unmeasured field -- that is SPLIT-LOCAL's question, and
# SPLIT-LOCAL has its own blind spots (see the scanner docstring). The four mechanisms and the
# hand-built control that proved the alias one are in docs/DIAGNOSTIC_GATE_AUDIT.md; #2628 then
# sharpened the scanner so three of the four stop producing findings at all.
#
# #2628 SHARPENED THE SCANNER, so this file shrank from 93 keys to 54. That is the direction which
# can silently disable a rule, so the rows were NOT regenerated: every surviving row was matched to
# its predecessor by the finding's own file:line, and every removal and re-key was read one at a
# time. Rows that re-keyed carry `[#2628 re-keyed; was ...]` so the previous requirement stays
# greppable.
#
# THE COUNTS, AND THE CONVENTION THEY ARE COUNTED UNDER -- stated because the obvious alternative
# gives different numbers and the two were mixed in an earlier revision of this header:
#
#   ROWS IN THIS FILE          39 unchanged  + 14 re-keyed + 1 new = 54
#   KEYS ON THE PREVIOUS FILE  39 unchanged  + 14 named as a predecessor by a marker
#                                            + 40 with no successor recorded = 93
#
# The convention is: count ROWS here, and account for the previous file's keys by whether a marker
# names them. Both numbers are then things a merger can GREP, which is the only property that
# matters -- these counts exist to tell whoever merges the sibling branch how many rows to expect,
# so a count they cannot reproduce with `grep` sends them hunting for a row nobody lost.
#   * `grep -c '\[#2628 re-keyed; was'` over the WHOLE file OVER-COUNTS, and no fixed number for it
#     is safe to write down: the header names the marker too -- including inside this very line --
#     so the total moves whenever the header is edited. The file #2628 shipped returned 16 against a
#     header that said 15, which is the same defect one revision earlier. `grep -v '^#'` first; that
#     returns 14 and is the number the counts above are stated in.
#   * The rejected alternative counts the PREVIOUS file's keys by which surviving row absorbed
#     them, giving 39 + 16 + 38 or 39 + 17 + 37 depending on whether a SPLIT-LOCAL and a TWO-GATE
#     sharing one line count as the same absorption. That it has two defensible answers is the
#     reason it is not used: a number nobody can re-derive identically is worse than a coarser one.
#     It is not wrong, and it is why 14 markers do not account for 54 departed keys -- see the
#     PROSPER_RTTLOG+PROSPER_RTTLOG_MIN_SUBMIT row below.
#
# What went away, by the mechanism that produced it:
#   * a negated env test read as a requirement -- the `PROSPER_NO_*` opt-out idiom, the default-on
#     PROSPER_RTT_PERTARGET|PROSPER_RTT_SINGLE_TARGET mode, and BOTH of the convention's own rule-2
#     shapes (guest_write_watch.cpp:1372's refusal, gpu_timeline.cpp:2288/:2742's half-a-pair
#     complaints), which were flagged for complying with #2149;
#   * an alias holding a compiled-in default (PROSPER_AGCSHADER_OUT);
#   * a stored lambda exporting its body's switches to its call sites (PROSPER_STAGE_FOLD_PROFILE);
#   * two call-site gates where one IMPLIES the other rather than disagreeing (hle_audio audio2log).
# Rule ONE's shape still produces findings and deliberately so: command_processor.cpp:1572's
# armed-state banner is conditioned on both switches by construction, and no lexical test separates
# "this banner explains the other switch" from "this report silently needs it". Both
# command_processor rows are still here.
# One row is NEW and is the rule working rather than decaying: with the spurious shared clause gone,
# SPLIT-CALL can see that diagnostic_elapsed_ms's three sites disagree.
#
# HOW THE JUDGEMENTS GOT ONTO THESE KEYS (#2572 merging second, after #2628). The keys are #2628's
# and the notes are #2572's, matched row by row rather than by taking either file whole -- taking
# one whole is the failure this file's own integrity check and the merge ORDER exist to stop.
# 39 rows took #2572's note for the identical key verbatim; 14 took the note of the predecessor
# named in their `[#2628 re-keyed; was ...]` marker, of which 10 were RE-ARGUED because the
# predecessor's argument was about a requirement the re-key removed -- most of them the `(any)` RTT
# amalgam, which is no longer what the writes are attributed to; 1 (diagnostic_elapsed_ms) is new
# under #2628 and keeps its own note. Line numbers in the notes were re-checked against the tree at
# merge time; the file has moved under them before and will again, so a note that has drifted is
# re-derived from the surrounding code, never trusted as an ordinal.
#
# Regenerate the key list with:
#     python3 tools/env/check_diag_gates.py <root> --emit-baseline
# then re-apply the classifications. Never let a regeneration silently downgrade a `defect` row to
# `unreviewed` -- that is the laundering this tool exists to prevent, performed by hand. Since #2628
# that sentence is also ENFORCED: the class counts below must match the rows, and the `unreviewed`
# count must equal UNREVIEWED_BUDGET in check_diag_gates.py exactly, in both directions. So a
# regeneration fails the gate, and paying debt down requires tightening the budget in the same
# commit. (Note that --emit-baseline currently writes its banner to stdout as well -- #2649.)
#   benign: 46
#   config-echo: 10
#   defect: 2
#   unreviewed: 0
#
SPLIT-CALL|frontends/shared/live/live_renderer.cpp|diagnostic_elapsed_ms|PROSPER_DUMP_PERSISTENT+PROSPER_PASS_LOG  # benign: the callee is a pure clock read (live_renderer.cpp:650-654 returns steady_clock::now() minus a function-local static origin) -- it holds no counter and prints nothing, so no call site can leave a producer unarmed. Its own comment (:646-649) states the shared origin is deliberate, so `ms:` windows in PROSPER_PASS_LOG and PROSPER_DUMP_PERSISTENT aim at one instant. Surfaced by #2628: the spurious clause the three sites used to share is gone, so the rule can now see they disagree. #2628
SPLIT-CALL|src/gpu/timeline/gpu_timeline.cpp|automatic_capture_bundle_gate_conflicts|PROSPER_CAPTURE_BUNDLE_AFTER_GUEST_LOG+PROSPER_CAPTURE_BUNDLE_TRIGGER_FILE  # benign: the callee re-reads the environment itself (gpu_timeline.cpp:2241 loops kAutomaticCaptureBundleGates calling getenv) and prints only names it measured in that loop, so it has no producer to leave unarmed. One entry point per gate is the design; the third call site (:3703, AT_PRESENT) is the same shape. #2572
SPLIT-CALL|src/host/image/exec_image_linux.cpp|arm_hwwatch|PROSPER_HWBP|PROSPER_HWWATCH+PROSPER_HWWATCH_ABS  # benign: three DELIBERATE entry points into one arming helper -- PROSPER_HWWATCH_ABS arms a fixed absolute VA at startup (exec_image_linux.cpp:3244-3249), PROSPER_HWWATCH arms [reg+delta] on the first PROSPER_HWBP exec-bp hit (requested at :3297, armed at :1779), and the HLE hook arms a slot discovered at runtime (:3155-3159). The helper prints only what it measured in that same call -- "perf W-watch FAILED addr=... errno=" (:533-534) or "armed W-watch at 0x... (fd=)" (:541-542) -- so neither switch is a producer for the other's printer. #2149
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_submit_query|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2327; defaults to -1, a sentinel the run-timing printer renders as "none" (submit_query_name, :5476-5488) and never as a query result. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_validated_bytes|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2325; accumulates bytes actually validated at :3439. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_validation_ms|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2324; accumulated at :3443 under `timing_enabled` and printed under `render_timing_detail` (:5463), i.e. PROSPER_RENDER_TIMING=detail, which implies it. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_watch_active|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2331; copied from the cache entry at :3393 and :3554. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_watch_disabled|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2332; copied from the cache entry at :3395 and :3556. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_watch_only|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2333; copied from the cache entry at :3397 and :3558. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_watch_query|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB+PROSPER_TEXTURE_WRITE_WATCH_MIN_KB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). The key's third name is DEFAULTED too: PROSPER_TEXTURE_WRITE_WATCH_MIN_KB is a size cutoff that falls back to 1024 KiB when unset (:3310-3315) and only decides whether a page watch is worth its setup cost (`cross_submit_watch_eligible`, :3316-3317). Declared :2328; assigned from the watch at :3478, and -1 is a sentinel the run-timing printer renders as "none" (watch_query_name, :5490-5502). #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|frontends/shared/live/live_renderer.cpp|resource_texture_watch_stability|PROSPER_NO_TEXTURE_DECODE_CACHE|PROSPER_TEXTURE_DECODE_CACHE_MB  # benign: neither name in the key is an arming switch, so these writes happen on a DEFAULT run. live_renderer.cpp:3286-3292 passes PROSPER_ENV_VALUE("PROSPER_NO_TEXTURE_DECODE_CACHE") != nullptr (:3289) and persistent_decode_limit (:3292) as two arguments of ONE call to persistent_texture_decode_cache_eligible(), and the scanner reads an argument list as a single conjunct: PROSPER_NO_TEXTURE_DECODE_CACHE is an opt-OUT, and PROSPER_TEXTURE_DECODE_CACHE_MB only overrides a budget that otherwise derives from host physical memory (the IIFE at :2143-2146; texture_decode_cache_limit_bytes(), :730-743, clamps physical/8 into 1..4 GiB when the override is absent). Declared :2329; copied from the cache entry at :3398 and :3559. #2572 [#2628 re-keyed; was `(any)`]
SPLIT-LOCAL|src/gpu/execute/gpu_executor.cpp|fresh|PROSPER_GPU_CAPTURE_RESOURCE_PROVENANCE|PROSPER_UDPROV  # defect: #2569 fresh_extent: prints 0, which reads as 'the bind programmed nothing'
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_after_compute_program|PROSPER_GPU_TIMELINE_CAPTURE_AFTER_COMPUTE_PROGRAM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_compute_program|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_COMPUTE_PROGRAM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_dispatch_threads_x|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_DISPATCH_DIM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_dispatch_threads_y|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_DISPATCH_DIM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_dispatch_threads_z|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_DISPATCH_DIM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_min_dispatches|PROSPER_GPU_TIMELINE_CAPTURE_MIN_DISPATCHES  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_min_draws|PROSPER_GPU_TIMELINE_CAPTURE_MIN_DRAWS  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_target_height|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_TARGET_DIM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_target_min_index|PROSPER_GPU_TIMELINE_CAPTURE_TARGET_DRAW_INDEX  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
SPLIT-LOCAL|src/gpu/timeline/gpu_timeline.cpp|select_target_width|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_TARGET_DIM  # config-echo: selector value echoed by [timeline]; 0 truthfully means UNCONSTRAINED
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_DETILE+PROSPER_DUMP_RAWTILE  # benign: both sites are snprintf() composing output FILE NAMES (live_renderer.cpp:4747, :4748) for the fwrite at :4749, not reports -- no field is printed that either switch could have measured. PROSPER_DETILE is read at :4718 to choose the de-tiled path; the dump arms on PROSPER_DUMP_RAWTILE alone (:4739). #2572 [#2628 re-keyed; was `PROSPER_DETILE+PROSPER_DUMP_RAWTILE+PROSPER_NODETILE`]
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_MRT_CENSUS+PROSPER_MRT_SHAPE_FOR  # benign: DEFAULTED -- PROSPER_MRT_SHAPE_FOR is an optional address FILTER that defaults to "" = record everything (live_renderer.cpp:6139-6142), so it is not a requirement; the sole arming switch is PROSPER_MRT_CENSUS (:6043), and every census map is incremented under the same gate that prints it. #2572 [#2628 re-keyed; was `PROSPER_MRT_CENSUS+PROSPER_MRT_SHAPE_FOR+PROSPER_RTT_PERTARGET|PROSPER_RTT_SINGLE_TARGET`]
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_RENDER_TIMING+PROSPER_RENDER_TIMING_DETAIL_MIN_SUBMIT  # benign: DEFAULTED -- PROSPER_RENDER_TIMING_DETAIL_MIN_SUBMIT defaults to 0 (live_renderer.cpp:5456-5457, :5397-5401). The detail lines are gated on `render_timing_detail`, i.e. PROSPER_RENDER_TIMING=detail (:1744), which implies `timing_enabled` (:1731-1735 with performance_timing_policy.hpp:14) -- so the spans they print are measured whenever they print. #2572
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_RTTLOG+PROSPER_RTTLOG_MIN_SUBMIT  # benign: PROSPER_RTTLOG_MIN_SUBMIT/_MAX_SUBMIT are an optional submit WINDOW defaulting to 0..INT_MAX (live_renderer.cpp:1551-1554), so that clause is not a requirement; the arming switch is PROSPER_RTTLOG alone (:1558). [#2628 re-keyed; was `PROSPER_RTTLOG+PROSPER_RTTLOG_MAX_SUBMIT|PROSPER_RTTLOG_MIN_SUBMIT`] -- THIS ROW ABSORBED THREE predecessor keys and the marker names the one whose note transfers. The other two are `PROSPER_GFXLOG|PROSPER_RENDER_REFVS|PROSPER_SKIP_DRAW+PROSPER_RTTLOG+PROSPER_RTTLOG_MAX_SUBMIT|PROSPER_RTTLOG_MIN_SUBMIT+PROSPER_RTT|PROSPER_RTT_PERTARGET|PROSPER_RTT_SINGLE_TARGET` and `PROSPER_RTTLOG+PROSPER_RTTLOG_MAX_SUBMIT|PROSPER_RTTLOG_MIN_SUBMIT+PROSPER_RTT_PERTARGET|PROSPER_RTT_SINGLE_TARGET`; the first one's note was deliberately NOT carried across, because it opens on a {GFXLOG|RENDER_REFVS|SKIP_DRAW} clause that came from the build_bds lambda alias -- exactly what #2628 removed, so the argument is about a requirement this row no longer has. MIN keeps its alias only because its ternary fallback is the literal 0 (:1551-1552) while MAX's is INT_MAX (:1553-1554), which alias_is_a_gate() retires. #2572
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_RTTLOG|PROSPER_RTTLOG_MIN_SUBMIT+PROSPER_RTT_GUESTPEEK  # benign: MODE + sub-selector. PROSPER_RTTLOG_MIN_SUBMIT/_MAX_SUBMIT are an optional submit WINDOW defaulting to 0..INT_MAX (live_renderer.cpp:1551-1554), so that clause is not a requirement; the arming switch is PROSPER_RTTLOG alone (:1558); PROSPER_RTT_GUESTPEEK is a real second switch (live_renderer.cpp:4131) but not a producer -- the non-zero byte census it prints is counted at :4158-4161, inside the same condition. #2572 [#2628 re-keyed; was `PROSPER_RTTLOG|PROSPER_RTTLOG_MAX_SUBMIT|PROSPER_RTTLOG_MIN_SUBMIT+PROSPER_RTT_GUESTPEEK+PROSPER_RTT|PROSPER_RTT_PERTARGET|PROSPER_RTT_SINGLE_TARGET`]
TWO-GATE|src/gpu/pm4/command_processor.cpp|report|PROSPER_GENERATION_GUARD+PROSPER_MB3_FREELIST_GUARD  # benign: stale_dma_change_report (command_processor.cpp:1703) prints a per-event record -- ordinal, address, build-pre, exec-pre, packet, timestamp -- all measured at the call. The two-variable requirement is a deliberate documented contract (:1715-1725: the DMA-init leg must not be re-armed without the MB3 release leg), not a producer/printer split. #2572
TWO-GATE|src/gpu/pm4/command_processor.cpp|report|PROSPER_INIT_TRIP+PROSPER_MB3_FREELIST_GUARD  # benign: this IS the convention's own rule-1 banner. command_processor.cpp:1572 prints 'INIT-TRIP NOT A CONTROL -- PROSPER_MB3_FREELIST_GUARD is on ... so member=0 here is structural, not a measurement'. A banner that states an armed state is by construction conditioned on both switches, so complying with rule 1 MAKES a TWO-GATE finding. #2572
TWO-GATE|src/gpu/execute/gpu_execute.hpp|report|PROSPER_CAPTION_DIAG+PROSPER_FRAME_DIR  # benign: the four sites are snprintf() composing OUTPUT FILE NAMES (gpu_execute.hpp:2091/2069/2073/2075), matched by the scanner's printf family; PROSPER_FRAME_DIR is the directory to write them to. The [caption] stderr report itself prints under PROSPER_CAPTION_DIAG alone (:2053). #2572
TWO-GATE|src/gpu/execute/gpu_executor.cpp|report|PROSPER_COMPUTE_TREE_WATCH_AUX+PROSPER_COMPUTE_TREE_WATCH_DUMP  # benign: all three sites are file-I/O status for the aux dump -- 'aux unreadable' (gpu_executor.cpp:9757), an snprintf path (:9766) and 'dumped aux -> path (ok|SHORT)' (:9774). _DUMP supplies the output directory and its absence returns at :9719 with no output at all; nothing measured is printed. #2572
TWO-GATE|src/gpu/execute/gpu_executor.cpp|report|PROSPER_DYNTRACE_FAIL|PROSPER_GFXLOG+PROSPER_UD_TAIL_ALIGN  # defect: #2146 [udtail]: the one report answering #305 also needs PROSPER_UD_TAIL_ALIGN, which the charter requires stay OFF [#2628 re-keyed; was `PROSPER_DYNTRACE_FAIL|PROSPER_DYNTRACE_FAIL_ADDR|PROSPER_GFXLOG+PROSPER_UD_TAIL_ALIGN`]
TWO-GATE|src/gpu/execute/gpu_executor.cpp|report|PROSPER_DYNTRACE_FAIL|PROSPER_RESDUMP+PROSPER_GPU_CAPTURE_RESOURCE_PROVENANCE|PROSPER_UDPROV  # benign: the printer sits INSIDE the producer's own gate -- `if (udprov_enabled())` at gpu_executor.cpp:6659 opens the block the [udprov] lines print from, so the provenance map is filled whenever they run, and an unrecorded register prints `@never` (:6666) rather than a numeral. Contrast row 54 (`fresh`, #2569): same function, same two switches, print 47 lines EARLIER at :6612 and OUTSIDE the gate -- that pair is what this review's discriminator was checked against. #2572 [#2628 re-keyed; was `PROSPER_GPU_CAPTURE_RESOURCE_PROVENANCE|PROSPER_UDPROV+PROSPER_RESDUMP`]
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_AFTER_COMPUTE_PROGRAM+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:525) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_AFTER_COMPUTE_PROGRAM itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_BUNDLE+PROSPER_GPU_TIMELINE_CAPTURE_CHECKPOINT_EVERY+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:400) for a malformed PROSPER_GPU_TIMELINE_CAPTURE_CHECKPOINT_EVERY; the BUNDLE conjunction is real (the sub-selector only exists inside a bundle request, :335) but nothing measured is printed -- an unset selector prints nothing at all. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_BUNDLE+PROSPER_GPU_TIMELINE_CAPTURE_DEPTH+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:342) for a malformed PROSPER_GPU_TIMELINE_CAPTURE_DEPTH; the BUNDLE conjunction is real (the sub-selector only exists inside a bundle request, :335) but nothing measured is printed -- an unset selector prints nothing at all. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_BUNDLE+PROSPER_GPU_TIMELINE_CAPTURE_MAX_UNIQUE_MB+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:352) for a malformed PROSPER_GPU_TIMELINE_CAPTURE_MAX_UNIQUE_MB; the BUNDLE conjunction is real (the sub-selector only exists inside a bundle request, :335) but nothing measured is printed -- an unset selector prints nothing at all. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_BUNDLE+PROSPER_GPU_TIMELINE_CAPTURE_START_TARGET_DIM+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:373) for a malformed PROSPER_GPU_TIMELINE_CAPTURE_START_TARGET_DIM; the BUNDLE conjunction is real (the sub-selector only exists inside a bundle request, :335) but nothing measured is printed -- an unset selector prints nothing at all. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_BUNDLE+PROSPER_GPU_TIMELINE_CAPTURE_START_TARGET_DRAW_INDEX+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:386) for a malformed PROSPER_GPU_TIMELINE_CAPTURE_START_TARGET_DRAW_INDEX; the BUNDLE conjunction is real (the sub-selector only exists inside a bundle request, :335) but nothing measured is printed -- an unset selector prints nothing at all. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_BUNDLE+PROSPER_GPU_TIMELINE_CAPTURE_TARGET_DIM+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:362) for a malformed PROSPER_GPU_TIMELINE_CAPTURE_TARGET_DIM; the BUNDLE conjunction is real (the sub-selector only exists inside a bundle request, :335) but nothing measured is printed -- an unset selector prints nothing at all. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_MAX_DISPATCHES+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:470) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_MAX_DISPATCHES itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_MAX_DRAWS+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:443) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_MAX_DRAWS itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_MIN_DISPATCHES+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:458) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_MIN_DISPATCHES itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_MIN_DRAWS+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:433) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_MIN_DRAWS itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_TARGET_DRAW_INDEX+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:423) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_TARGET_DRAW_INDEX itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_COMPUTE_PROGRAM+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:512) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_WHEN_COMPUTE_PROGRAM itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_DISPATCH_DIM+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:537) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_WHEN_DISPATCH_DIM itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/gpu/timeline/gpu_timeline.cpp|report|PROSPER_GPU_TIMELINE_CAPTURE_WHEN_TARGET_DIM+PROSPER_GPU_TIMELINE_CAPTURE|PROSPER_GPU_TIMELINE_CAPTURE_SUBMIT  # benign: a VALIDATION message (gpu_timeline.cpp:412) -- it prints only when the operator supplied a malformed value for PROSPER_GPU_TIMELINE_CAPTURE_WHEN_TARGET_DIM itself, states the accepted range, and carries no measured field. Unset -> nothing to complain about and nothing printed. #2572
TWO-GATE|src/hle/input/hle_pad.cpp|report|PROSPER_PAD_SCRIPT+PROSPER_PAD_SCRIPT_RELOAD  # benign: both sites announce a MODE rather than report a measurement -- log_error() of the stat error read one line above (hle_pad.cpp:165) and 'live route reload enabled for <path>' (:166). Printing either without PROSPER_PAD_SCRIPT_RELOAD armed would be the defect. #2572
TWO-GATE|src/host/image/exec_image_win.cpp|report|PROSPER_NULL_PAGE+PROSPER_NULL_PAGE_LOG  # benign: the log reports the EVENTS of the behaviour the other switch enables -- every field is measured at the print (exec_image_win.cpp:465 increments the counter, :466 prints it), and with PROSPER_NULL_PAGE off there genuinely are no emulated null reads. Not #2146's shape: the report is about the mutation itself, not an independent question the mutation contaminates. #2572
TWO-GATE|tests/fixtures/render_runner.h|report|PROSPER_BACKEND_TIMING_WINDOWS+PROSPER_RENDER_TIMING  # benign: the printer is inside the producer's gate. render_runner.h:8037 returns unless `timing_mode.log`, which is PROSPER_RENDER_TIMING (:3563), and performance_timing_policy.hpp:14 makes `log` imply `measure` -- so the clocks feeding these 13 lines are always running when they print. PROSPER_BACKEND_TIMING_WINDOWS only adds the every-25-calls cadence; arming it alone prints nothing. #2572
TWO-GATE|tests/fixtures/render_runner.h|report|PROSPER_DRAW_ISO+PROSPER_ISO_AT  # benign: MODE + its required argument -- PROSPER_ISO_AT is the pixel coordinate the isolation pass cannot run without (render_runner.h:7746-7748), not a producer. Every printed field (per-draw blend/mask/viewport, the per-kill results) is computed inside the same block. #2572
TWO-GATE|tools/boot_trace/boot_trace.cpp|report|PROSPER_CRASHPEEK+PROSPER_PEEK_CLASS  # benign: MODE + its required argument. PROSPER_PEEK_CLASS is a sub-selector of the PROSPER_CRASHPEEK post-fault dump (boot_trace.cpp:350) and supplies the addresses to peek; every printed value is read from guest memory in the same loop (:427), so there is no producer whose arming differs from the printer's. Unarmed -> no output at all. #2572
TWO-GATE|tools/boot_trace/boot_trace.cpp|report|PROSPER_CRASHPEEK+PROSPER_PEEK_CODE  # benign: MODE + its required argument. PROSPER_PEEK_CODE is a sub-selector of the PROSPER_CRASHPEEK post-fault dump (boot_trace.cpp:350) and supplies the addresses to peek; every printed value is read from guest memory in the same loop (:457), so there is no producer whose arming differs from the printer's. Unarmed -> no output at all. #2572
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_DUMP_RTGROUPS_ADDR+PROSPER_DUMP_RTGROUPS|PROSPER_DUMP_RTGROUPS_RGBA  # benign: BOTH gates are load-bearing, not a split producer/printer. The dump arms at live_renderer.cpp:7501 and the address filter is read at :7504; the report at :7533 names which MRT SLOT that filter matched. With no ADDR filter there is no match to report, and with no dump variant armed nothing is written for the match to describe. Every field it prints -- the wanted address, the matched slot, the pass's slot-0 base -- is computed at :7504-:7512 under the same condition that prints it, so it cannot report unarmed state as a measurement. #2542
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_GPU_REPLAY_LIVE_TARGETS|PROSPER_GPU_TIMELINE_CAPTURE+PROSPER_RENDERER_MIP_CHAIN_LOG+PROSPER_RENDER_SCALE  # benign: PRODUCTION-MEASURED -- the arming switch is PROSPER_RENDERER_MIP_CHAIN_LOG alone (live_renderer.cpp:4131). Every field the report prints is computed OUTSIDE that gate and would be computed with it unset: `mip_chain` at :4100 and its `level_count`/`level_ids` are ordinary render state, and the limit comes from prosper::test::persistent_color_target_limit(). So arming the switch whose name matches the question does not leave you in silence. The PROSPER_RENDER_SCALE and PROSPER_GPU_REPLAY_LIVE_TARGETS|PROSPER_GPU_TIMELINE_CAPTURE clauses are enclosing-scope alias artifacts, not requirements of this statement -- PROSPER_RENDER_SCALE is read at :3064 and :7692, neither of which encloses :4172. #2996
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_READBACK_WHY+PROSPER_READBACK_WHY_MIN_SUBMIT  # benign: DEFAULTED -- PROSPER_READBACK_WHY_MIN_SUBMIT is an optional lower bound whose unset value is literal 0ull (live_renderer.cpp:7733-7737), so `g_this_submit >= diagnose_min_submit` is unconditionally true and that clause is not a requirement. The arming switch is PROSPER_READBACK_WHY alone (:7739). Same mechanism as the PROSPER_RTTLOG+PROSPER_RTTLOG_MIN_SUBMIT row above. #2996
TWO-GATE|frontends/shared/live/live_renderer.cpp|report|PROSPER_RENDER_TIMING_DETAIL_MIN_SUBMIT+PROSPER_RENDER_TIMING|PROSPER_RENDER_TIMING_DETAIL_CAPTURE  # benign: DEFAULTED + MODE -- PROSPER_RENDER_TIMING_DETAIL_MIN_SUBMIT falls back to literal 0 when unset (live_renderer.cpp:6369-6371, and again :6454-6455), so it is a window bound rather than a requirement. PROSPER_RENDER_TIMING_DETAIL_CAPTURE (:2043) joins the existing detail mode by ALTERNATION, so it widens how the diagnostic can be armed instead of adding a second thing that must be. This is the re-keyed sibling of the PROSPER_RENDER_TIMING+PROSPER_RENDER_TIMING_DETAIL_MIN_SUBMIT row above, whose #2572 argument transfers unchanged. #2996
